# 🔁 796. Rotate String

## 🧠 Problem Understanding

796. Rotate String

Given two strings s and goal, return true if and only if s can become goal after some number of shifts on s.

A shift on s consists of moving the leftmost character of s to the rightmost position.

For example, if s = "abcde", then it will be "bcdea" after one shift.
 

Example 1:

Input: s = "abcde", goal = "cdeab"
Output: true
Example 2:

Input: s = "abcde", goal = "abced"
Output: false
 

Constraints:

1 <= s.length, goal.length <= 100
s and goal consist of lowercase English letters.


## 🚀 Key Idea

Rotation doesn't change characters — only their order.

So:

* Length must be equal
* Characters must match in some rotated order

---

# ✅ Approach 1: Trick (s + s contains goal)

## 💡 Idea:

If we double the string:

```
s = "abcde"
s+s = "abcdeabcde"
```

All rotations of `s` will appear inside `s+s`.

👉 So just check:

```
goal is substring of s+s
```

---

## ✅ Java Code

```java
class Solution {
    public boolean rotateString(String s, String goal) {
        if (s.length() != goal.length()) return false;
        return (s + s).contains(goal);
    }
}
```

## ✅ Python Code

```python
class Solution:
    def rotateString(self, s: str, goal: str) -> bool:
        return len(s) == len(goal) and goal in (s + s)
```

## ⏱ Time Complexity

* `O(n)` average (substring search)
* Space: `O(n)`

---

# 🔁 Approach 2: Brute Force Rotation

## 💡 Idea:

Rotate string one by one and compare.

---

## ✅ Java Code

```java
class Solution {
    public boolean rotateString(String s, String goal) {
        if (s.length() != goal.length()) return false;

        String temp = s;

        for (int i = 0; i < s.length(); i++) {
            temp = temp.substring(1) + temp.charAt(0);
            if (temp.equals(goal))
                return true;
        }

        return s.equals(goal);
    }
}
```

## ✅ Python Code

```python
class Solution:
    def rotateString(self, s: str, goal: str) -> bool:
        if len(s) != len(goal):
            return False

        temp = s
        for _ in range(len(s)):
            temp = temp[1:] + temp[0]
            if temp == goal:
                return True

        return s == goal
```

## ⏱ Time Complexity

* Each rotation: `O(n)`
* Total: `O(n²)`
* Space: `O(n)`

---
🔁 Approach 3: Brute Force Rotation (Totally Valid Here)
💡 Idea:

Try all rotations and check each time.

👉 Since n is small, brute force is safe and simple

✅ Why This Approach is GOOD Here
✔ Easy to understand
✔ Easy to code in interview
✔ No extra algorithms needed
✔ Works within constraints (no TLE)

👉 This is actually the most beginner-friendly approach

✅ Java Code
class Solution {
    public boolean rotateString(String s, String goal) {
        if (s.length() != goal.length()) return false;

        String temp = s;

        for (int i = 0; i < s.length(); i++) {
            temp = temp.substring(1) + temp.charAt(0);

            if (temp.equals(goal))
                return true;
        }

        return s.equals(goal); // handles case when no rotation needed
    }
}
✅ Python Code
class Solution:
    def rotateString(self, s: str, goal: str) -> bool:
        if len(s) != len(goal):
            return False

        temp = s

        for _ in range(len(s)):
            temp = temp[1:] + temp[0]

            if temp == goal:
                return True

        return s == goal
---

# ⚡ Approach 4: KMP (Efficient String Matching)

## 💡 Idea:

We again use:

```
text = s + s
pattern = goal
```

But instead of `.contains()`, we use **KMP algorithm** for efficient search.

---

## 🧠 Why KMP?

* Avoids re-checking characters
* Works in linear time

---

## ✅ Java Code

```java
class Solution {
    public boolean rotateString(String s, String goal) {
        if (s.length() != goal.length()) return false;

        String text = s + s;
        int[] lps = buildLPS(goal);

        int i = 0, j = 0;

        while (i < text.length()) {
            if (text.charAt(i) == goal.charAt(j)) {
                i++;
                j++;

                if (j == goal.length())
                    return true;
            } else {
                if (j > 0)
                    j = lps[j - 1];
                else
                    i++;
            }
        }

        return false;
    }

    private int[] buildLPS(String pattern) {
        int[] lps = new int[pattern.length()];
        int len = 0, i = 1;

        while (i < pattern.length()) {
            if (pattern.charAt(i) == pattern.charAt(len)) {
                len++;
                lps[i] = len;
                i++;
            } else {
                if (len > 0)
                    len = lps[len - 1];
                else
                    i++;
            }
        }
        return lps;
    }
}
```

---

## ✅ Python Code

```python
class Solution:
    def rotateString(self, s: str, goal: str) -> bool:
        if len(s) != len(goal):
            return False

        text = s + s
        lps = self.build_lps(goal)

        i = j = 0

        while i < len(text):
            if text[i] == goal[j]:
                i += 1
                j += 1

                if j == len(goal):
                    return True
            else:
                if j > 0:
                    j = lps[j - 1]
                else:
                    i += 1

        return False

    def build_lps(self, pattern):
        lps = [0] * len(pattern)
        length = 0
        i = 1

        while i < len(pattern):
            if pattern[i] == pattern[length]:
                length += 1
                lps[i] = length
                i += 1
            else:
                if length > 0:
                    length = lps[length - 1]
                else:
                    i += 1

        return lps
```

---

## ⏱ Time Complexity

* Building LPS: `O(n)`
* Searching: `O(n)`
* Total: `O(n)`
* Space: `O(n)`

---

# 📊 Comparison Table

| Approach | Idea                   | Time  | Space | Difficulty |
| -------- | ---------------------- | ----- | ----- | ---------- |
| 1        | `(s+s).contains(goal)` | O(n)  | O(n)  | ⭐ Easy     |
| 2        | Rotate & compare       | O(n²) | O(n)  | ⭐ Easy     |
| 3        | Same as 2              | O(n²) | O(n)  | ⭐ Easy     |
| 4        | KMP Algorithm          | O(n)  | O(n)  | ⭐⭐⭐ Hard   |

---
